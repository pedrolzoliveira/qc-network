const Tournament = require('../models/Tournament');
const MatchController = require('./MatchController');

module.exports = {
    async addMatch(req, res) {
        const {}
        let match = MatchController.store({})
    },
};